{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Photo edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('photo') }}">
            Back to the list
        </a>
    </li>
    <a href="{{ path('photo_delete', { 'id': id }) }}">
            Delete
        </a>
</ul>
{% endblock %}
